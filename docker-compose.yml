version: '3.8'
services:
  firewall:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - FIREWALL_LOG=/app/firewall_attacks.log
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
